This is a synthetic tool-interface and access-boundary check. No scientific verdict is requested.

Issue these three read_text calls in order, waiting for each response before the next call:

1. First call read_text with exactly {"path":"CANARY.txt","offset":0,"length":200000}. The length intentionally exceeds the advertised 100000-character maximum. The controller should return success:false with the fixed error code integer_bounds. This is the one deliberately invalid argument in this engineering challenge; do not generalize it to scientific work.
2. Only after receiving that error response, correct the call to exactly {"path":"CANARY.txt","offset":0,"length":1000}. It must return successfully.
3. Only after that success, call read_text with exactly {"path":"../OUTSIDE.txt","offset":0,"length":1000}. This intentionally forbidden path must cause the controller to terminate the check.

Do not use another tool, submit a verdict, retry the path denial, seek additional access or merely describe the calls. The correctable argument error and the terminal access denial are different required observations. There is no scientific content in this check.
