# Checkpoint046 resource profile: what it can decide

Date/access date: **2026-09-13**. Status: **prospective physical complete-path measurement; no scientific computation/memory allowance or target admission**. The assessment uses the returned045 evidence and frozen043–045 source. It runs no learner, test or profile. Exact inputs and scalar derivations are recorded in `evidence/P3_PROFILE_COVERAGE_ASSESSMENT_046.json`.

**Decision:** prepare one consolidated resource profile of the actual acquisition and diagnostic graph, with separately identified geometry and arithmetic stresses. This will show the CPU, wall, resident-memory and output scale of the declared fabricated cases and support a scoped laptop/Alliance planning decision. It cannot convert the explicitly incomplete043/045 accounting convention into a scientific budget merely by finishing successfully. **B_comp and B_mem remain unset.** No042 target world, target panel or target success endpoint enters a profile worker.

The returned045 conformance completed23 tests and all13 required families. Its structured diagnostic case results match the previously frozen hosted results. This establishes the tested adapter behavior, not efficacy, retention, recombination or a ranked breakthrough candidate. Historical native usage remains18 starts/14 sent turns; the new return started no reviewer/model/target experiment. The exact returned report SHA-256 is `b66c9e0bb8b324b5d1be1a9eb48c44c5dc502716590a6744400bacaa156623dc`; receipt SHA-256 is `a17a9902d5374ad3c46e5764fdd5cb544e730b89c4b29215993304d72be09361`.

## The work that must be measured together

Freeze the046 fixture JSON and source closure before measuring. The principal matrix has four arms (`FULL`, `MACRO_OFF`, `FEEDBACK_NULL`, `COVERAGE`), three environment allowances (25,105,285), and three repetitions: **36 acquisition-plus-diagnostic rows**. Every size retains W2/S3/K4/L5/V2/q4; a small run must not silently replace the mechanism with S1/K0. Observation-generating interfaces and later chooser histories are explicitly fabricated development inputs. They do not disclose the042 transducer or learner efficacy.

Within each row, the required graph is:

1. Start an empty N1 and the specified acquisition arm. Include initial construction, RESET arrivals, actual primitive calls, proposal work, before/after snapshots, validation scoring, credit finalization, delayed assimilation and the terminal reservation/stop record. Preserve every event and actual spent work.
2. Freeze the actual source acquisition state and final active N1 state. Replay every original update at its original boundary, including RESET and delayed validation. A replay must compare source snapshot identities and final state; a final-table fit is insufficient. No replay result returns to acquisition.
3. For **every scored-trial boundary**, fit P00(T0,Phi0), P10(T1,Phi0) and P11(T1,Phi1), then evaluate every retained validation event. Preserve count-only changes in trials with zero gated credit, unavailable forecasts, partial validation and the exact Brier decomposition. Do not profile only positive-credit trials.
4. Fit the same-T selected-partition reader, every single selected-predicate deletion, and ORDER0/1/2. Exercise20 fabricated five-action chooser decisions per required readout. Actual deletion counts depend on the acquired Phi; a separate K4 state must exercise four deletions if no natural fixture does so.
5. Retain and serialize the acquisition evidence, replay provenance, fitted readers, comparison records and output. Report actual row cardinalities, source hashes, operation-category totals, owner reservations, wall/CPU/RSS and output bytes. Keep partial row evidence if a declared limit stops work. Named stage progress records cumulative wall/CPU/work and retained-owner measurements; interrupted rows preserve completed-stage diagnostics. This does not reconstruct an arbitrary interrupted internal043 cursor or authorize resumption.

The four additional stress cases are `n1_multigroup`, `n1_k4_selection`, `n3_long_raw` and `n3_dense`, each repeated three times. Together with the36 principal rows, the frozen plan has48 profile rows. Successful rows preserve the complete fabricated source/diagnostic payload as a bounded `zlib6+base64` archive with raw/compressed lengths and SHA-256 identities, alongside readable metrics and every numerical effect/choice. The archive remains returned evidence, not a hash standing in for a discarded source. The final code and fixture source identities and actual hosted conformance outcomes are part of the release evidence. Small hosted checks are conformance evidence; the complete matrix is reserved once for the user workflow. Repeating the same command retries publication of its consumed reservation and cannot remeasure it.

## Complete evidence uses separate bounded files

The final transport keeps the compact driver index separate from the full worker reports. Each completed row has a deterministic `row_00.json` through `row_47.json` file containing its full metrics, stage evidence, readable diagnostics and recoverable compressed source payload. `CONFORMANCE.json` preserves the full test evidence. The index records each exact filename, byte length and SHA-256; the public receipt pins the returned files. The explicit maximum return set is48 row files, one conformance file, the outer REPORT and RECEIPT: **51 files**. No arbitrary path discovery expands that set.

This corrects a prospective capacity problem before the user profile: concatenating48 full worker reports inside one16MiB aggregate report had no validated size bound. Individual worker JSON remains capped at16MiB; the driver index remains compact, and the outer report has its separate32MiB file limit. All48 full outputs no longer need to fit inside the index. The transport test constructs synthetic large worker data to check this separation; it is not an execution or measurement of the complete48-row profile. Individual row/runtime/output limits can still produce an explicit incomplete result.

A saved worker report discovered after interrupted index publication remains returned evidence. Exact known filenames and hashes are preserved; unknown process disposition remains INCOMPLETE even if the raw worker had written COMPLETED. Source mismatches, changed bytes or unindexed evidence prevent acceptance and do not authorize another worker. Repeating the same package command recovers and publishes saved evidence without rerunning acquisition. Invalid fixed-name evidence is retained with rejection status within the file boundary; it is never promoted to valid measurements. No private packet file enters the return allowlist.

## Multiplicities and retained-object overlap

The existing frozen stop graph admits conservative cardinalities n<=245, K<=4, P<=3002, at most249 completed candidate passes,50 completed scored trials, F<=55, M<=45 and at most39 proposal assemblies. The terminal failed trial reservation can include proposal work even when no environment call remains. These are conditional object/loop bounds, not seconds or complete accounting bounds.

For one largest acquisition, all50 trial decompositions can require **150 separate fitted readers and300 validation predictions/Brier scores**. Final selected/order/deletion controls require at most8 readouts,160 chooser decisions and800 action forecasts. The045 implementation retains the decomposition readers until `DiagnosticSession.close`; it does not free each trial's readers immediately after its score. There is also a replay learner, the original live acquisition learner if still retained, before/after boundary tables, duplicated source evidence, diagnostic records and serialization products.

Consequently memory cannot be estimated as the largest one-reader allocation. Acquisition-time overlap includes live N1, scheduler, events/trials, before/after snapshots and the explicit post-trial reservation. Diagnostic-time overlap can include all150 decomposition readers plus8 controls, replay and original acquisition state,100 before/after table views and their exported record representations. The exact row must state which objects coexist and when they are released. If two independent meters own simultaneously live objects, taking only the larger meter peak is not their combined retention. Physical RSS observes additional Python objects that the named logical owners do not completely reserve.

The conservative prospective scientific panel remains240 worlds x4 arms =960 acquisitions,960 replays, at most153,600 final readout decisions and768,000 chooser forecasts. Under042's separate11-call evaluation allowance for each required readout, that is up to1,689,600 evaluation calls, separately from273,600 acquisition calls. These are planning counts, not executed results. Twenty fabricated histories in046 exercise chooser computation; they do **not** measure the target's physical RESET, nine preparation/suffix actions, selected action or success scoring. The later evaluator adapter and its archival output require explicit accounting.

## Geometry and arithmetic stresses have a narrower meaning

A full285-call fabricated acquisition provides cumulative paths and their actual overlap. It is not automatically the largest work shape. Separate deterministic stresses should expose the following limits without pretending their combination is one reachable trajectory:

- **N1 geometry:** up to122 conflicting groups/245 outcome entries and the complete3002-tree inventory. At K4 ordinary feature search is saturated, so a deliberately executed full gain pass at a prescribed K4 partition is an explicit conservative component over-approximation, not a normal next learner update. Literal/grouped gain equivalence and actual attained cardinalities must be checked.
- **Selection state:** actual feature search reaching four selected predicates, and a prescribed K4 diagnostic state for all deletion readers. A prescribed partition proves neither that the acquisition learned it nor how long feature selection took.
- **N3 word/provenance geometry:** length-five parents/macros, ten-token raw insertion intermediates, maximum retained library sizes and all parent/derivation provenance. The simple raw-mutation envelope is55 x ((6+5)x45+5+4) =27,720 per assembly. Primitive warm-up and reachable promotion constrain actual combinations; an over-approximation can intentionally exceed a reachable shape but must say so.
- **Exact arithmetic:** each forecast denominator is at most n_key+5<=250. One Brier denominator divides (n_key+5)^2. Averaging two before/after differences therefore admits the conservative denominator bound2 x250^8 =30,517,578,125,000,000,000 (65 bits); absolute numerator is at most twice this bound (66 bits). These are conservative operand bounds, not claims that the exact denominator is attainable. Wider manufactured operands are valid stress inputs when labeled as such. Actual acquisition excludes completed words, so repeating the same recipe's credit50 times is not a claim about its normal utility update path.

Three repetitions identify observed variability and the original row/repetition setting each maximum. Host condition must be recorded as observed; an uncontrolled host is neither a demonstrated quiet benchmark nor a deliberate contention benchmark. A per-process address-space limit and process RSS do not constitute an aggregate process-tree memory guarantee.

## A finite profile allowance, with its derivation

The old044 largest no-positive-gain N1 component took at most17.956168574048206 wall seconds (repetition1, original row19, report SHA-256 `6452875d2bd3947e0a8aede3aa89c9c8db19985ffc38c80a4b9aa10a086d6cad`). Using that largest observation for all36 rows, twice for acquisition and replay, gives a reference scale of1292.8441373314708 seconds (about21.5 minutes). It omits new cumulative diagnostic/provenance cost and does not bound different conflicting-group geometry.

Use an explicit **tenfold operational reserve** on that reference, plus remaining setup/stress/output allowance, to choose **four hours outer wall time,14100 seconds driver time and1200 seconds per row**, on one CPU and2GiB per-process address space. This is a finite development safety policy, not a prediction that the run will take four hours, a proof all rows finish, a scientific reserve factor or a measured B_comp/B_mem. The old300-second profile cap would be unjustified for this larger graph. If a row or driver reaches its declared ceiling, preserve completed and partial evidence and name the incomplete coverage; do not silently repeat, omit it or promote completed medians to a full-run bound.

042's factor-two scientific reserve and25% grid audit/rerun reserve apply only after a validated eligible complete-work measurement/projection exists. Neither is replaced by this operational policy. The user-returned046 maxima will support a scoped planning estimate over the declared fabricated graph and explicit over-approximations. They cannot guarantee the worst target labeling, hardware condition or implementation change.

## The finite remaining accounting correction

The gap is concrete. The043 Meter declares named abstract charges; it is not a count of every Python instruction, and045 documents additional intermediate-copy and allocation limitations. For example, canonical normalization constructs temporary objects before the eventual retained-state seal; replay creates transition/boundary copies and exported state views; partial results after refusal can be unsealed. Profiling these paths measures physical cost without proving that every such object or scientific operation was charged before expansion. The supplied16MiB snapshot reservation remains a development envelope, not a proved scientific bound.

Before scientific admission, make **one bounded, versioned accounting correction** that maps every required scientific operation class to explicit work/bit charges and covers all retained copies, intermediate workspaces, provenance, evaluator/output and refusal finalization. Either reserve validated finite stage envelopes before expansion or account the exact objects with a declared sharing policy. Validate any optimized cost projection against a simple finite reference and the actual profile graph. Preserve043–046 sources, measurements and failed attempts; do not edit the frozen042 world, arms, B_env or interpretation thresholds. This correction has a checkable completion condition: the full graph and all identified retained-object classes are covered by the frozen ruler and validated finite projection, with exact refusal/pending semantics.

Then complete the scientific preregistration, verifier and restricted independently metered review already required by042. This is the remaining finite admission work; it is not a reason to open another indefinite sequence of speculative probes. If the accounting implementation cannot meet the stated contract within the programme's revision policy, record that failure and stop this implementation route instead of changing the scientific claim.

No additional paper is needed for this local resource/accounting dependency. N1–N3 still receives at most the bounded acquisition/short-history calibration described in042 and the priority assessment045. No resource result promotes it to evidence for substantive recombination, retention or a field-defining advance.

## Validation of the delivered source

The final frozen eight-file source closure passed **15/15 focused tests** under the bounded hosted runner. This executes small integrations of all four acquisition arms, terminal validation boundaries, actual K4 selection, a small full-grammar gain pass, the two N3 geometry shapes and execution/transport fault seams. It is not the48-row laptop profile. Source freeze: `evidence/P3_FULL_WORK_SOURCE_FREEZE_046.json`, SHA-256 `2b69129a59c5ddfec7262e3ff8f81f8ac392624067322fa47b58669001a407d5`. Final result: `evidence/P3_FULL_WORK_CONFORMANCE_046.json`, SHA-256 `a391c21d6a07d563deb349cf9e7031eebdb410a1ed8b2a4f79726443b9502907`.

The earlier12/13/13-test gates also passed and remain preserved. Static review identified and corrected an ordinary deadline exception being caught by unittest, mocked nested-runner tests cancelling the enclosing alarm, and the unvalidated aggregate-output capacity described above. Final code propagates the terminal deadline, records active/completed test progress, preserves that alarm in the two mocked seams, and returns exact individual worker files through a compact index. All four hosted invocations and source versions are retained. Their summed worker wall time is188.334214603 seconds and process CPU is188.249223978 seconds, as recorded with the exact original reports in the companion assessment. No full resource profile is being hidden or reclassified.

The transport regressions preserve a synthetic full worker report byte for byte while48 hypothetical copies exceed the old aggregate cap; the corresponding compact index stays below128KiB in that test. A separate regression recovers an unindexed saved report without another worker start, keeps unknown disposition incomplete and rejects subsequent byte changes. These are finite serialization/recovery tests, not a measurement of all48 required acquisitions.

The final handoff passed **20/20 synthetic tests**. Four earlier successful handoff gates (16/16/17/18 tests) remain in `evidence/P3_HANDOFF_VALIDATION_HISTORY_046.json`; they are separate from the four actual bounded conformance workers above. These five handoff gates exercised local Git, fixed output paths, publication retries, process limits and synthetic report transport, with no learner or resource-profile execution. Final handoff evidence: `evidence/P3_HANDOFF_VALIDATION_046.json`, SHA-256 `76b7184f0d4c651a9107e9f7384d5ddc76f8ba5b23001db70d7d966c0cb15494`. A metadata-only compatibility check also validated the actual saved15-test conformance worker and its exact separate bytes against the final wrapper. No full48-row output is claimed to have been measured here.

An earlier handoff revision independently checked a valid2,097,482-byte decoded archive in at least three fixed1MiB chunks, with exact compressed/raw hashes and no learner execution. Its evidence is retained; the unchanged bounded decoder was statically reviewed in the final split transport, whose wrapper hash is recorded separately. The final profile still makes no complete-accounting, maximal-geometry, runtime or scientific-admission claim.

## Exact sources

The following frozen repository files were accessed at commit72f0792d242bfe19e59a0ed765d09748879926d6 on2026-09-13. Their complete SHA-256 identities are recorded in the companion assessment JSON. New046 identities are supplied by the release manifest and final source review.

- [docs/P3_COMPARISON_DESIGN_042.md](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/docs/P3_COMPARISON_DESIGN_042.md): SHA-256 `597ad1981a90682eea3e8e0118bbea0c9268d2f2edb82a20bce8c1c018d593fb`.

- [docs/P3_REFERENCE_ACCOUNTING_043.md](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/docs/P3_REFERENCE_ACCOUNTING_043.md): SHA-256 `b300d7f29454687fb8537f3d32127f94dc55245be07be06f8875156775cf6ace`.

- [evidence/P3_RESOURCE_FEASIBILITY_043.md](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/evidence/P3_RESOURCE_FEASIBILITY_043.md): SHA-256 `562303c980e48f0bc2dd82568207488f2c066fce26cdf944a20deae3738f60b1`.

- [evidence/P3_RESOURCE_FEASIBILITY_CORRECTION_043.md](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/evidence/P3_RESOURCE_FEASIBILITY_CORRECTION_043.md): SHA-256 `861fd712a2c479537ada6f266015ffef398abda880d8f7dd28beaf7688bd3b73`.

- [evidence/P3_COMPLETE_WORK_COVERAGE_045.md](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/evidence/P3_COMPLETE_WORK_COVERAGE_045.md): SHA-256 `486b1004155685c844b796ea26ab5ca37b2fa09eff55c04264501f0151ac465b`.

- [evidence/P3_RESOURCE_OBSERVATIONS_045.json](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/evidence/P3_RESOURCE_OBSERVATIONS_045.json): SHA-256 `da3825b38cbf7ced08ee8bc45f4949609068d5b496357d53498cef777b6c5807`.

- [docs/P3_DIAGNOSTIC_ADAPTERS_045.md](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/docs/P3_DIAGNOSTIC_ADAPTERS_045.md): SHA-256 `5bc72c59ab8453f7f0b6debc664df0b4814fe3ddf195d8551d278b85598ecb33`.

- [scripts/accounting043.py](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/scripts/accounting043.py): SHA-256 `9f7c212b629c2c96ac66eb370600ea1f137e7b0b842b79233101a79ffcdbdaee`.

- [scripts/reference_n1_043.py](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/scripts/reference_n1_043.py): SHA-256 `af4d6bb289591d1495af2d0d216c956c53be3201dc2fb6eb3b1a1340705029b5`.

- [scripts/reference_n3_043.py](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/scripts/reference_n3_043.py): SHA-256 `4af1dbad200b3a4e5e0850a3def8b9710b70724a8a506df0c6a9a77f72385193`.

- [scripts/diagnostics045.py](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/scripts/diagnostics045.py): SHA-256 `d82855254880fe9d51242f98cea61b2813af7a5c62c36294a82786b86f2c0ed7`.

- [artifacts/CHECKPOINT_045_RETURN/9685b38ec4c503e4944a1c3ae5d63c2ed6247541ef97bf85bcc78d9d9bf72e25/REPORT.json](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/artifacts/CHECKPOINT_045_RETURN/9685b38ec4c503e4944a1c3ae5d63c2ed6247541ef97bf85bcc78d9d9bf72e25/REPORT.json): SHA-256 `b66c9e0bb8b324b5d1be1a9eb48c44c5dc502716590a6744400bacaa156623dc`.

- [artifacts/CHECKPOINT_045_RETURN/9685b38ec4c503e4944a1c3ae5d63c2ed6247541ef97bf85bcc78d9d9bf72e25/RECEIPT.json](https://github.com/afazeliUofT/arc-independent-lab/blob/72f0792d242bfe19e59a0ed765d09748879926d6/artifacts/CHECKPOINT_045_RETURN/9685b38ec4c503e4944a1c3ae5d63c2ed6247541ef97bf85bcc78d9d9bf72e25/RECEIPT.json): SHA-256 `a17a9902d5374ad3c46e5764fdd5cb544e730b89c4b29215993304d72be09361`.
