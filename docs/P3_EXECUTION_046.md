# Checkpoint046 consolidated resource profile

Save ARC_Independent_Lab_046.zip in Windows Downloads and paste the supplied WSL-home command. It finds Downloads, verifies the whole ZIP, copies and extracts it under `~/ARC_Independent_Lab/delivery/P3_CHECKPOINT_046`, verifies the repository and inputs, performs one bounded fabricated workload profile, and pushes the report, receipt, conformance and every saved case result to GitHub.

This run is longer than the045 checks. Its outer safety ceiling is four hours on one CPU; this is not a duration promise. Keep the WSL terminal open and the laptop on AC. Completed rows and partial evidence are saved incrementally. Repeating the command reuses the first reserved execution and retries publication; it does not restart measurements or reset043–045 reservations.

The profile covers acquisition plus the attribution and reader diagnostics, with declared small/typical/largest fixtures and stress cases. All worker inputs are fabricated. The042 target world, panel and target outcomes are absent. No native reviewer, model API, GPU or HPC job starts. Resource measurements remain separate from scientific budget admission and efficacy.

The ZIP includes all required public sources, accepted045 full return, historical governing/scientific records and the preserved172-file private paper packet. Private files are verified but excluded from the worker and Git publication. Read `reports/P3_RETURN_045_AND_PROFILE_ROUTE_046.md` and `docs/P3_COMPLETE_WORK_PROFILE_SCOPE_046.md` for the decision this run supports and the remaining accounting limits.

A successful publication may contain an incomplete profile. It will say so plainly and provide a GitHub receipt; preserve all existing files and send the final status if stopped. The receipt identifies each returned file and its checksum. Full worker results are saved separately so the combined report does not have to duplicate their complete payloads. No additional paper is needed for this step.
