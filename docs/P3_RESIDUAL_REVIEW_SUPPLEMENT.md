# Required supplement to the first Phase 3 review packet

2026-09-07. Read this with `docs/P3_AUDIT_REVIEW_PACKET.md`, not as a replacement for its evidence or enforcement requirements. No review has been invoked. This supplement preserves the earlier pinned record and supplies explicit corrections before a verdict is requested.

The exact input set is the union of `evidence/P3_AUDIT_REVIEW_MANIFEST.json` and `evidence/P3_RESIDUAL_REVIEW_MANIFEST.json`. Verify both and all listed files. The supplement manifest pins the original manifest itself, all four new analytical notes and the PI synthesis. Treat prior and current PI claims with equal suspicion. Source-version and full-method access requirements remain in force.

In addition to the original separate C1–C4, joint and observer assessments, answer:

1. Does C4's retained state suffice to reconstruct both scopes in the counterexample? If so, does the earlier language confuse selected response with erased evidence? Check the cost of reconstruction and the absence of a specified joint factual veto.
2. Are the wrapper's seed-relative boundary and bounded hitting-set mappings exact? Check empty clauses, encounter IDs rather than unique descriptors, collisions away from the seed, the supplied length bound, complete search and the distinction between a local table certificate and a causal diagnosis.
3. Does T1-ref establish only the stated matched implementation equivalence? Does any historical priority claim illegitimately import C4 into its comparator? Assess KWIK-Rmax as an actual integrated antecedent without asserting identity with C2/C4.
4. Is the current-model API observer exactly residualization with the same information access? Check Lovell's specific working-paper theorem assumptions and the no-information zero-padding argument separately from the direct algebra. Do not treat the current weights used as regression response coordinates as old training labels.
5. Is query identifiability proved on the stated unrestricted domain? Check the pseudoinverse decoder, the state-perturbation factor and the finite-codebook limitation. Separate exact formula, implementation tolerance, numerical robustness and autonomous invocation.

Use the original permitted verdict vocabulary and output channel. A correct negative audit can receive GO for completion of the audit while its reduced operations receive separate adverse novelty dispositions. An unestablished historical whole-wrapper identity is neither proof of novelty nor proof of nonnovelty. Do not promote the residual diagnostic result into a tested mechanism.

There is still no independently enforced reviewer runtime. A shared-filesystem consultant, a prompt requesting read-only behavior, or advertised client flags do not meet the requirement. The separate runtime must be verified before this task is invoked; capability reports are preparation only. The strongest three objections and PI-side checks are explicit in `reports/P3_RESIDUAL_AUDIT_2026-09-07.md` and the ledger.
