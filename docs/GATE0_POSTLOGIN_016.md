# Post-login observation and receipt acceptance

Prepared 2026-09-08T00:12:44.310606+00:00. This is Gate 0 engineering within Phase 3, not a scientific result.

The supplied sign-in receipt passes the exact published wrapper schema and provenance pins. It reports native completion and credential-path presence. The original bytes are preserved at `artifacts/GATE0_HUMAN_SIGNIN_OBSERVATIONS/20260907_001/REPORT.json`, SHA-256 `3293ab230f5c9e3f32f5bbd4f6a45abe2d603021ec08d6bc7af0dea230020a0c`. The acceptance record is `evidence/GATE0_HUMAN_SIGNIN_015_RECEIPT_REVIEW.json`, SHA-256 `c66502fc2a3d940627ea7574a4bde1eaa89abef010bf5a00aff121f457280cfe`. User-supplied execution is not independent runtime attestation. No credential or raw login output was requested.

The next operation bundles the useful account questions in one human invocation. It preserves existing home/config/backend/policy provenance and exact existing cache inputs. A local dummy-file test must prove the required file-write and parent/readonly-file denials before the native process starts. It then permits only the native client's existing credential file to be refreshed in place, with other writes confined to the fixed project run. Resolver symlink targets are mounted as exact readonly file contents at their original lookup names; this preserves contents, not original symlink topology. No broad home, Codex-home, /etc or session-bus grant exists in the native plan. The dummy test alone reads system runtime directories to execute Python, with no real credential present.

Inherited networking permits native startup authentication and metadata work. It is not an endpoint allowlist. The fixed protocol contains no model, thread, login/logout, config-write or reset/credit-consumption operation. It does not send scientific inputs. Optional quota/catalog errors are retained without discarding earlier observations; unexpected requests, invalid provenance, partial runs or deadlines stop rather than retry. A completed receipt is verified and reused before host inspection or launch.

Parent output is filtered: account-kind/plan enums, explicit Codex-limit numeric windows, booleans for the requested model and recognized effort strings, and typed configuration observations. Raw email/account IDs/tokens, policy strings, catalog descriptions, cursor values, native diagnostics, and stdout/stderr hashes are excluded. Only the report is shared; runtime directories remain private. A live rate-limit response can establish authenticated access to that endpoint, not entitlement to a model or the hosted conversation's allowance. Catalog entries may be cached. Cloud-policy closure and full reviewer independence remain unverified.

Controlling scope `fbd7ca325593c9c8de5573417faf1381dbf669b80218f85e4a31ae519beacc61` specifies the process limits and exact exception. The native file save truncates in place; interruption during refresh persistence is a native limitation. The narrow permission is required because the already-approved sign-in scope covered only the completed sign-in, not this subsequent credential-writing native process. Detailed exact-version sources, URLs and access dates are in `docs/GATE0_POSTLOGIN_SOURCE_2026-09-07.md` (SHA-256 `30bfadf85637b92077b5093abea4f46a068bbbb7f8162dfb34f025d1fb1115de`).

Engineering validation: `artifacts/GATE0_POSTLOGIN_ENGINEERING/20260908_001/MANIFEST.json` SHA-256 `63435cd1fa5239bfc9d3fa45f8af57c7d8aae87d99455fef871449c49beb9e1c`. The controlling test run passed 35 targeted tests; raw command, output, source hashes and report are retained. The real namespace attempt in this hosted environment failed before the child because the required proc setting was unavailable; this is preserved, not described as a pass. The WSL dummy-file prerequisite addresses this exact remaining observation without another human round trip. No native Codex process, real credential access or model was used by these tests.
