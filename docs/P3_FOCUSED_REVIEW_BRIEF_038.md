# Focused analytical review with supplied primary methods — checkpoint038

Date: 2026-09-11. This successor brief supersedes checkpoint037's source-access exclusions for the two newly supplied papers. It preserves the checkpoint035 operations and checkpoint036–037 analyses as historical inputs. This is a PI-authored assessment task, not an independent verdict or an executable model scope.

## Decision and objective

The funded question is why agents fail to acquire useful knowledge through unfamiliar interaction, retain its usefulness across discontinuities and recombine it for unfamiliar demands. A new descriptive object, contrast, or intrinsic score is insufficient by itself. The decision is whether the frozen second-candidate operations and their explicit limitations justify a separately specified, discriminating successor comparison, or instead justify returning to generation. Novelty and usefulness are distinct questions.

Read the six governing files in the order in `docs/governing/00_START_HERE.md`, then the approved amendments and frozen diagnosis/specifications. The diagnosis ranks candidate bottlenecks; it does not establish their prevalence across all contemporary AI. Neither a bounded historical success nor a counterhistory for a proposed operation resolves that broader question.

The input manifest is `evidence/P3_FOCUSED_REVIEW_MANIFEST_038.json`. Its public `inputs` and separately hashed `private_sources` define the actual reading packet. The user-supplied full texts must be available through a read-only reviewer route. A PI reading receipt is not a substitute for the reviewer's own access. Shared-tools consultation is PI assistance and cannot issue the required independent verdict.

## Claim tasks

Retain the claim IDs and qualifications from checkpoint037, with the following complete assessment contract. For every claim supply the actual proposition checked, premises, derivation or counterhistory, relevant input paths and SHA-256 digests, and `SUPPORTED_WITH_SCOPE`, `REFUTED_WITH_SCOPE`, or `UNRESOLVED`. A label applies to the proposition, not the candidate as a whole.

| ID | Claim to assess | Required boundary |
|---|---|---|
| R1 | Reconstruct N1's pair-coverage update and the history where positive pair gain leaves predictions unchanged. Using Chvátal's full paper, assess the equal-cost greedy-step reduction in the new supplement. | Freeze the retained transition table, predicate grammar and selected prefix. Syntax size is a tie rule, not a positive column cost. Separate uncovered but uncoverable pairs. State exactly when the static harmonic bound applies; do not export it to the whole capped online learner or prediction. |
| R2 | Reconstruct N2's fixed-panel signatures and query precedence; assess the example where failure outside a full panel does not change role membership. | `TRANSFER_CONJECTURE` is defeasible. Refute no invented universal substitution promise. Historical equivalence to other named methods requires their actual methods, absent from this two-paper addition. |
| R3 | Redo the single-block N1–N3 finite-credit proof: at most `K - |Phi_after_warmup|` post-warm-up trials can have available numeric nonzero fresh utility. | Each such trial must append a distinct predicate. Validation and incomplete trials may consume capacity without credit. The trial reaching capacity can still receive credit. Unavailable is not zero; inherited utility, coverage, macros and count updates can continue. |
| R4 | Trace the constant-world zero-credit history through the actual scheduler. Assess the positive count-fitting concern separately. | The positive concern is a conditional local state/score construction until a complete reachable reset/action/update/validation history is supplied. Do not silently elevate it to a whole-loop counterexample. |
| R5 | Check the one-completed-post-warm-up-trial rule per literal word and inherited parent score; identify unspecified cross-block choices. | Do not choose warm-up restart, score carryover/deletion or recalibration on behalf of the frozen design. A finite useful discovery burst remains a live defense. |
| R6 | Check N4's six-endpoint relation, its weak bridge to future useful knowledge, and the full-method comparison with Pierce–Kuipers. | Distinguish a new endpoint contrast from restoring the original response. Distinguish historical acquisition of sensory/action/control representations from N4's literal word library, FIFO crossed-trial scheduler and reset contract. Do not turn nonidentity with one paper into novelty. |
| R7 | Check N5's PI-constructed supervised-expression normalization and source/target information channels. | Reader weights can retain historical label information. This internal normalization alone does not establish named whole-method historical priority, target transfer or useful recovery. |

The two new supplements are propositions to challenge. In particular, test the strongest counterarguments: Chvátal's static rule need not identify N1's whole dynamic procedure; historical control transfer need not answer the funded retention/recombination problem; a finite credit burst need not imply a failed agent.

## Source-specific assessment

`evidence/P3_PRIMARY_SOURCE_INVENTORY_038.json` records identities, source hashes, access date, page conventions and reading scope. Both supplied works are published journal articles, not preprints.

- Chvátal (1979): verify the selection ratio against the actual printed algorithm and distinguish its theorem from N1's extra rules. The paper itself credits earlier unweighted results. Do not claim Chvátal first invented unweighted greedy set cover without investigating those earlier sources.
- Pierce and Kuipers (1997): check the implemented context heuristic, feedback templates, retained/erased modules in §§6.2–6.3, failure experiments, future composite-action proposal, user-guided final discrete tour, and proposed later mapping levels. A discussion section describing what could be built is not an execution result.

For historical reassessment, state which restrictions are documented by the primary paper and which 2026 implications are PI inferences. Greater compute can enlarge a search; it does not establish an adequate representation, informative sampling, a reset affordance or usefulness under a new question. No claim about why the wider field abandoned a line of work is established by these two papers.

The supplied methods close the specific access gaps from the earlier request. They do not supply every source used in checkpoint036 or complete a global priority search. Claims depending on other unprovided full methods remain unresolved unless the actual source version is added through a separately verified route. No further paper is currently necessary for the bounded internal claims and these two method comparisons.

## Decision output and execution boundary

The assessment must end with a scoped recommendation: specify a successor comparison, return to generation, or identify a concrete unresolved dependency. To recommend a comparison, identify what future decision would improve because of acquired knowledge, what information and interactions both sides receive, and what observation would disconfirm the proposed advantage. Do not tune a candidate or choose numerical success thresholds after seeing results. Resolve discontinuity and cross-block semantics prospectively before claiming retention or recombination.

Any independent overall `GO / REVISE_ONCE / KILL / SUSPEND_FOR_DEPENDENCY` applies only to acceptance of this analytical assessment. It is not candidate efficacy, global novelty clearance, approval of `PROGRAMME.md`, or an integrated N1–N5 guarantee. No valid independent session means no verdict file.

Checkpoint038's helper stages and verifies inputs and returns a source-staging receipt to GitHub. It does not launch a reviewer. The old032 controller is pinned to the first audit's packet, claims and observer-auditor contract; changing its packet path alone cannot execute this scope. A successor must supply a new prompt, claim/output schema, admitted source broker, fresh scientific context, protected verifier, enforced read-only input boundary and finite resource accounting. Reuse verified engineering only where the actual boundaries are unchanged. This is implementation work under standing authorization, not a new request for the user's permission.

Return actual execution artifacts, claim assessments, reviewed input hashes, boundary/resource receipts and partial outputs to GitHub. Never infer usage from a missing report or reconstruct missing evidence. Preserve the exhausted032 accounting and all frozen scientific inputs.
