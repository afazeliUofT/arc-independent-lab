# Checkpoint043: one reserved development profile, with its full result returned

Save the single `ARC_Independent_Lab_043.zip` package in Windows Downloads and paste the accompanying command at WSL home. The command discovers the actual Windows Downloads setting, verifies the published whole-ZIP SHA-256, and uses the existing `~/ARC_Independent_Lab` checkout. The observed account path is `/home/afazeli2006/ARC_Independent_Lab`; the active branch must be `main` and its origin must be `afazeliUofT/arc-independent-lab`.

The package is copied and extracted under ignored `delivery/P3_CHECKPOINT_043/<ZIP-SHA256>/bundle`. Every member must have its expected hash and ordinary file shape. Matching prior extraction files are reused; changed files are preserved and cause a stop. The workflow fetches GitHub and verifies the release's exact public Git objects and every packaged private-source hash. The release must descend from the accepted 042 return commit `c66f764955c6cfe17d78ae593c290b4ae949e4d3`.

This step executes reference development code on fabricated conformance and workload fixtures. It measures implementation behavior and resource use before any target comparison. It does not execute the 042 target transducer, goals, instance panel or evaluation, and it does not run a native model, reviewer or HPC job. New native starts and sent turns remain zero; the cumulative recorded totals remain 18 starts and 14 sent turns. These engineering observations do not establish scientific efficacy, novelty, a complete-work budget or target-experiment admission.

Before launching, the workflow copies only the explicitly pinned profile source closure into `delivery/P3_043_RETURN_WORKFLOW/<release-manifest-SHA256>/execution_source`: the accounting module, N1 and N3 reference modules, profile driver, fabricated-fixture configuration and three declared test modules. It verifies the copied hashes and makes these source files read-only. The private papers and042 target sources are outside this execution directory. The child uses isolated Python flags `-I -B -S`, a minimal environment, this source tree as its working directory, and a separate ignored output directory. The driver must import its declared siblings explicitly. This arrangement is directory and import separation; it is not an operating-system isolation guarantee.

The child has one available CPU, a 2 GiB address-space limit, CPU soft/hard limits of 290/300 seconds and a 300-second parent wall deadline. The driver reserves an internal 270-second budget to write partial results. Individual child files, including stdout and stderr, are limited to 8 MiB. At the parent deadline the workflow kills the child's process group and reaps it; termination and cleanup may take additional time outside the work allowance. A Linux parent-death signal is requested so ordinary parent termination also kills the direct child. These controls and observed process behavior are recorded; they are not a general security proof.

The first launch is reserved atomically in the ignored per-release workflow directory. **A reservation is never replenished automatically.** The full first execution result is saved atomically before publication. Repeating the command after a failed commit or push reuses those exact bytes and does not profile again. A reservation found without a saved result produces an interrupted return, preserving any valid partial child report and leaving unknown start or completion observations explicitly unknown. It never relaunches. An exact already-published report and receipt can also restore the local saved result without another execution.

The child writes `profile_output/REPORT.json`. The public execution report preserves the complete valid child report under `profile_report`, with its canonical hash, permitted source hashes, caps, observed exit status and elapsed time. Missing or malformed child output produces a categorical failure field. Private stdout and stderr contents are never copied into GitHub; only their byte counts and hashes are reported. Arbitrary exception text, credentials, environment values and paper contents are excluded.

The only public output paths are:

- `artifacts/CHECKPOINT_043_RETURN/<release-manifest-SHA256>/REPORT.json`, containing the saved execution record and full child report.
- `artifacts/CHECKPOINT_043_RETURN/<release-manifest-SHA256>/RECEIPT.json`, binding that report to the release, source closure, accepted 042 return and execution boundaries.

Both use canonical JSON: sorted keys, two-space indentation, ASCII escaping, no nonfinite numbers and a final newline. Publication stages exact Git object bytes, commits both files together, pushes and verifies the remote branch. Git hooks are disabled for these fixed publication operations. Synchronization is fast-forward only; changed outputs, unrelated edits or unpublished commits, and diverged branches are preserved and cause a stop. A completed profile returns exit status 0. An incomplete profile can still return its report and receipt successfully to GitHub while the command exits with status 1; repeating it retries publication or displays the same incomplete result, without executing more work.

`evidence/P3_HANDOFF_VALIDATION_043.json` records the focused hosted handoff checks. Synthetic fixtures test one-reservation behavior, interrupted and partial results, exact commit/push recovery, remote result reuse, the extracted launcher and real child process controls. They execute no reference learner or target panel. Complete-package validation is separate and must use an explicitly bounded development fixture; it must not silently run the full laptop profile in the hosted environment. Hosted checks do not establish laptop Windows interoperability or GitHub authentication. The returned GitHub report will provide the laptop's actual outcome.

The standing one-ZIP and WSL-home delivery preference remains in `docs/USER_HANDOFF_PREFERENCES_2026-09-11.md`. The supplied papers remain private in the package. No additional paper is required for this development profile.
