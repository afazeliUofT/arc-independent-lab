# Finite reviewer scope and evidence boundary

Prepared 2026-09-08; no native review has run. Controller SHA-256 `187cfab78d43736728294f45ee8a6004590766490fe0af98e8b711be71e734f1`; scope SHA-256 `99d7f62d60b3384b5bb83923047b390f7f7e6e8d90c2654b33d996101755d5ea`.

The parent starts a pinned Codex 0.151.0 native process in the existing empty-root bubblewrap plan. It preserves the original HOME/CODEX_HOME, exact configuration/policy origins and read-only cache/resolver/CA inputs. Only fresh stage runtime/log directories and the single existing auth file are writable. The latter is a same-path native bind, not a parent-readable copy or writable home-directory grant. Credential content is never opened, hashed or exported by the Python parent. In-place native refresh has an interruption/I/O risk; the parent creates no credential backup.

Networking is inherited host networking for existing-subscription operations, not an endpoint allowlist. The model has no project or private-paper mount; a separate parent owns the evidence broker. Empty environments plus final effective source-sensitive controls disable native shell/write/Code Mode host, external tools, hooks, memory and collaboration. Broker tools remain directly exposed. The notification validator detects control changes but is not itself the proof that a native side effect cannot happen. That argument depends on exact source effects, effective controls, the mount boundary and actual synthetic refusal together.

The synthetic stage receives only CANARY.txt and a fixed allowed-read then traversal-read challenge. Only the actual unchanged broker's canonical-path refusal, a successful first read, process reaping and unchanged protected host inputs admit science. Science gets new process/state/thread/broker directories and a neutral prompt referring to the frozen audit tasks; it inherits no PI conversation or synthetic thread. No resume/fork exists. Read-only input hashes are checked by the broker before and after every dispatch. Source and full frozen hashes are also checked before entry to the scientific stage. The parent can expose the fixed auditor and validate the output schema but cannot author a scientific verdict.

The active native model must be the exact requested gpt-6-astra with the highest recognized supported effort, fixed provider, read-only permissions and no instruction-file origins. The model catalog is collected in at most eight pages/800 rows; opaque cursors stay in memory. Absent model, incomplete catalog, changed Pro plan, nonnull managed requirements or mismatched controls stop with useful filtered evidence. No silent downgrade is permitted. This route deliberately makes no claim that ChatGPT product branding proves the native model identifier exists.

The complete operative ceilings are in the pinned scope: 600 seconds synthetic and 3600 seconds science including four seconds cleanup each; 16/512 broker calls; 30-second parent RPC timeout; 32 MiB per native file and 256 MiB polled runtime tree; 64 MiB synthetic stdout, 512 MiB science stdout and 64 MiB stderr; 24 MiB frame and 32 MiB outbound queue. CPU soft limits are 120/600 seconds with one second hard-limit grace. Tree polling is not an atomic disk quota; the wall deadline is process-session time, and finite host reads/hashes around it add overhead. No numeric ceiling represents subscription tokens or scientific novelty thresholds. No claim of continuous human attendance is made merely because a command is explicitly launched.

Safe session receipts are written before later postchecks. Raw native protocol, configuration values and stderr are not retained; known error categories and observed process outcomes are retained. This follows the existing credential-privacy contract and limits diagnosis. Broker arguments/receipts concern the manifested evidence only. A created verdict remains byte-identical even when execution admission later fails, and both status and hash are recorded. Reuse validates the report checksum, pins, saved session receipts and any verdict; extra or changed files stop. Partial attempts never automatically rerun.

Source basis: the exact upstream release commit is [78c290807ce710180111df227df3b7a4fe845452](https://github.com/openai/codex/tree/78c290807ce710180111df227df3b7a4fe845452). Archived retrieval URLs/access dates are in `artifacts/GATE0_REVIEWER_EFFECT_SOURCE/20260908_001/MANIFEST.json`, SHA-256 `88f6567051f17865584d5b0a90c9d655c3c0819b77a25d636333eb71363cdee1`, and `artifacts/GATE0_ACCOUNT_PLAN_SOURCE/20260908_001/MANIFEST.json`, SHA-256 `f47ece02e29f93aeb14cc08ea1fc53109e13eb197465699c9ba6a4f516c70704`. These local archives were rechecked 2026-09-08; the source was not freshly fetched during this integration. The embedded protocol schemas are `artifacts/GATE0_CODEX_INTERFACE/20260907_001/schemas/experimental.json`, SHA-256 `dd4014851ef7c60ecacfa5fa4d1a9f02d7dcc2aada93c83091a717b27b753d63`. Read-only sandbox schemas allow type/networkAccess, not an invented readOnlyAccess extension. Matching settings events are source-compatible; whether identical overrides always emit such an event was not established from the archived emitter selection.

Engineering validation: `artifacts/P3_FINITE_REVIEW_VALIDATION/20260908_001/REPORT.json`, SHA-256 `9c630973d1e4bde12e8d0b8f584f8499b615c4ea097f4c2f243c2ea6e9c6eef1`. Prior mount-denial report: `artifacts/GATE0_POSTLOGIN_OBSERVATIONS/20260908_001/REPORT.json`, SHA-256 `4aa0757da0d56a4db65ca7a33cf15d7454129c21fab7283457261d3a8ebcbba7`. Neither is an already-completed scientific verdict.
