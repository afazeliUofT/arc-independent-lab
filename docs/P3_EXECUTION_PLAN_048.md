# Checkpoint048: first accounting correction unit

Date: 2026-09-14. Parent: `f25777a324ac49b9828b4fdf155749c7d1b80626`.

The current request authorizes continuation, package delivery and GitHub publication. Full prior chat transcripts could not be retrieved: the personal-context service returned `Personal context is disabled.` The six current attachments were completely read in their prescribed order. Continuity is reconstructed from the canonical repository, not represented as a full reading of either prior conversation. No other programme was inspected.

## Independently verifiable advancement

Implement the first unit of the existing047 correction: a finite global ledger and pre-expansion canonical codec, together with a source-derived terminal call-graph bound and independent finite reference. Existing scientific adapters and043–047 records remain historical inputs. This unit is not the whole accounting correction, an adapter integration, a prospective scientific budget, or independent admission. It must never turn a passing unit test into a claim that the eleven047 operation/owner coverage rows are complete.

This is implementation and finite conformance, not another component or full-work profile campaign. No target environment, learner acquisition, native reviewer, model client, GPU or HPC job will run. The old046 reservation will not be reused. The optional047 synchronization receipt is not a dependency.

## Pre-execution validation scope

Freeze these bounded test families before executing the new unit:

- Ledger: global overlapping disjoint owners; separate acquisition/diagnostic/apparatus channels; reservation versus spent work; no spent-work refund or allowance increase; first-refusal latch; no scientific continuation after refusal; prepaid, bounded terminal evidence and idempotent cleanup.
- Codec: finite scalar, signed integer, escaped Unicode string, Fraction, list/tuple, set, mixed-key map, record and union fixtures; exact bytes against the frozen043 canonical function; structural length upper bounds from schema metadata; insufficient workspace refusal before normalization; invalid and recursive inputs rejected within finite schema validation.
- Static bounds: literal finite enumeration/dynamic programme versus algebraic formulas on small boundary domains and the frozen042 domain; distinguish retained records from event-bearing boundaries, full from partial validation, and mutation assembly attempts from completed trials. These scripts do not invoke scientific learners or schedulers.
- Handoff: exact source identity and safe extraction; canonical origin and clean fast-forward ancestry; a single saved result and exact report/receipt publication; retry publication without repeating completed conformance; interrupted run and dirty/unrelated local work protection. Git tests use disposable repositories within this lab and no live user publication.

Each conformance subprocess has a 300-second wall limit. At most three corrective conformance attempts per task; retain failure output and source identities before changing code. No optional repeated broad testing: run the frozen unit suites, the handoff suite, and one actual extracted-package/local-Git seam when code is final. A defect found by review can justify a targeted regression inside that finite scope. Tests perform no remote model invocation. Syntax compilation and read-only source/evidence inspection are not measurements of a learner.

## Acceptance and remaining work

Acceptance is limited to exact source-pinned unit conformance and structural derivation. All historical sources must remain byte-identical. Source and output hashes, actual execution usage, any failures, and final package identity will be recorded. Shared-workspace consultations are engineering checks, not formal independent scientific verdicts.

Even after unit acceptance, actual N1/N3, replay, diagnostics, evaluator and transport paths still require their closed schemas, guarded owner lifetimes, actual operation charges and refusal seams. Full archived046 graph domination, changed-path physical eligibility, prospective common allowances, final preregistration/verifier/programme, and restricted independently metered admission remain dependencies. No `B_comp`, `B_mem`, target permission or whole-correction completion is inferred from this unit.

The user-facing command will run only the two frozen unit suites and publish their report and receipt. It will not silently run any earlier package or start a profile. Receiving that report is useful platform verification; it is not a scientific result.
