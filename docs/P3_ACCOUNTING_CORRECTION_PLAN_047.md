# One finite accounting correction after the046 return

Date: **2026-09-14**. Source inspected: commit `15574d706eab28c1e099aba8447d527eb5047d91`. Status: **implementation contract; accounting closure, numerical scientific allowances and target admission remain unestablished**. This assessment executes no learner, profile, reviewer, model or target. Same-workspace consultation is not independent review.

The046 return closes the requested physical fabricated-profile handoff. It does not retrospectively complete the043 logical ruler. The next implementation step is one versioned correction of the enumerated allocation, work and terminal-record gaps below, followed by a finite reference comparison and the already required independent admission gate. Another unconstrained profile campaign, another paper request or another change to the042 scientific contrast is not the next step.

## 1. Decision and scope

Keep the035/042 scientific mechanism and the adopted043 numeric AST order, canonical JSON representation, chronological encounter timing, credit gate and all four arms. Preserve043–046 source, successful measurements, failed attempts and publication history. Implement the correction in new versioned modules; do not overwrite the historical modules or silently substitute the unimplemented binary codec/immutable-arena proposal in `P3_REFERENCE_ACCOUNTING_DRAFT_043.md`.

The smallest coherent route is **conservative storage envelopes reserved before each bounded stage, combined with explicit work charges for the actual executed operation classes**. A single common ledger must cover every simultaneously live scientific owner. Reserve work capacity separately from spent work if a stage needs a work escrow; an unused reservation is not executed work. Direct COVERAGE and suppressed branches must retain their actual cost savings. An envelope is not permission to charge a cheap arm for an unexecuted expensive algorithm, and old043 operation totals are not corrected totals.

Use the existing literal full-copy ownership policy. Do not introduce content interning, reference sharing discounts, compressed scientific state, streamed deletion of required evidence or a new feature-search implementation in this correction. Those changes would add independent proof obligations. Logical bytes and bounded apparatus overhead remain separate from Python RSS; the corrected ruler need not recursively charge its own counter instrumentation or claim to count every interpreter instruction.

## 2. Frozen finite domain and available bounds

The domain is one042 acquisition block, one five-token observation field, five opaque primitive actions and W2/S3/K4/L5/V2/q4. The following are conditional structural bounds from the frozen stop graph, not observed maxima or elapsed-time bounds:

| Object or operation | Bound and qualification |
|---|---|
| Acquisition environment calls |285, including RESET and validation |
| Retained transitions / selected predicates / full literal tree inventory |245 /4 /3002 |
| Completed full candidate passes |249; interrupted candidate work remains separately charged |
| Completed scored trials / recipe records / macros |50 /55 /45 |
| Proposal assemblies |At most39, including terminal proposal work before a failed trial reservation |
| Raw mutations in one conservative assembly |`55*((6+5)*45+5+4)=27,720`; raw insertion words can have10 tokens |
| Distinct retained nonempty words of length at most5 |At most `5+25+125+625+3125=3,905`; this is a vocabulary ceiling, not an attained queue size |
| Parent occurrences / derivation records in one assembly |Each has at most27,720 generated occurrences; deduplication can reduce parent entries |
| Trial decomposition |At most150 fitted readers and300 validation forecasts/Brier scores for50 completed scored boundaries |
| Final selected, deletion and ORDER controls |At most8 readers,160 decisions and800 forecasts per acquisition |
| Evaluation environment allowance |11 calls for each required decision: RESET, nine preparation/suffix actions and one selected action |

The implementation must separately enumerate a terminal partial scored boundary. The bound on **completed** trials must not silently be used as the bound on every allocated trial record, cursor, partial boundary or fitted-reader attempt. Derive these terminal multiplicities from the fixed call-reservation graph before allocating an envelope. An incomplete boundary is preserved with its own status; it never receives a completed-block mean.

An N1 forecast denominator is at most250. A complete before/after mean-credit denominator is conservatively bounded by `2*250^8=30,517,578,125,000,000,000` (65 bits), and its absolute numerator by twice that bound (66 bits). These are reduced-result bounds. Intermediate cross products, Euclidean steps, identity calculations and repeated accumulations require their own input-derived widths. In particular, do not use66 bits as a blanket bound on every temporary rational operand. The normal scheduler excludes completed words; a manufactured repeated-credit stress is not an acquired repeated-utility path.

These cardinalities, finite string vocabularies, fixed hashes and grammar constructors permit a **static schema-size derivation without running a learner**. They do not yet establish a total memory or work envelope. The existing16MiB post-trial snapshot reservation,16MiB worker report cap and128MiB decoded archive cap are development/transport ceilings, not proved scientific retained-state bounds. No numerical B_comp or B_mem can be validated merely by relabeling those ceilings or the046 observed maxima.

## 3. Required operation and owner corrections

The table defines the finite source-coverage checklist. Each row must appear in the correction manifest with the new guarding entry point, work-price rule, size expression, owner lifetime and reference-check identifier.

| Frozen source operation | Identified gap and required owner | Guard before expansion; terminal behavior | Acceptance evidence |
|---|---|---|---|
| `accounting043._normal`, `_nodes`, `canonical`, `Meter.encode`, `retain` | Normalized containers, set/map ordering keys, ordering workspace, JSON text and encoded bytes can exist before their final debit/reservation. `_nodes` itself traverses before its aggregate charge. | Derive size/node/ordering bounds from the closed schema and retained metadata; reserve all overlapping codec owners and precharge the defined traversal/output operations before constructing them. Seal size only within the reservation. | Tagged fractions/maps/sets and all permitted schemas give byte-identical043 canonical output; a deliberately too-small workspace refuses before normalization; independent length traversal never exceeds the bound. |
| `N1.__init__`, `_seal`, `state`, `_reserve`, inventory/truth/groups/count rebuilds | Exported state views and old/new rebuild overlap are not all separately reserved. A post-operation seal cannot retroactively authorize an expansion. | Reserve each bounded output/workspace using current cardinalities or a proved maximum before constructing lists, tuples, dictionaries or trees. Retain source owner until replacement commits. | Full inventory,245-record geometry, count-only updates and incomplete candidate cursor have covered owners; each replacement is refused before mutation when its reservation cannot fit. |
| `N1.snapshot`, `new_block`; `fullwork046.complete_chain` final-state export | `state()`/serialization precede snapshot reservation; deep copies and final-state exports add overlapping objects.042 uses one block, so unused cross-block behavior need not be admitted. | Guard state view, raw bytes and full snapshot copy independently. Derive the after-trial envelope from the schema; transfer capacity atomically to the snapshot owner without an unreserved gap. | Before/after source identities and final live state remain equal to043 at sufficient allowance; failed snapshot leaves source unchanged and releases only its prepaid temporary owners. |
| `Scheduler.state`, `_reserve_growth`, `_mutations`, `select`, `add_completed` | State/plain exports, raw invalid words, proposal index, queue/provenance and sort workspace overlap; some operands/copies are created before `_reserve_growth`. | Cover all27,720 possible raw mutation attempts,10-token intermediates, actual surviving queue/provenance and sort workspace. Charge only assemblies/branches actually invoked. Preserve terminal proposal cost before failed trial reservation. | Mutation traversal, parent/derivation multiplicity, score/tie order and arm differences agree with a simple finite enumerator; length rejection and queue replacement cannot allocate first. |
| `Acquisition._observe`, `_trial`, `_scored_trial`, `_assimilate_validation`, `run`, `state` | Incoming event/prediction/history representations, completed-response recovery, pending queues and final exports can grow around a refusal. Issued-action encounter timing has an explicitly unresolved interruption edge. | Before a physical call, preallocate the issued record, maximum permitted response and completion/pending footer. Make action issue and its encounter record one prepaid commit. Preserve returned observations before fitting; replay delayed encounters in order. Reserve terminal-only record capacity independently of scientific continuation. | Faults before issue, after issue, after response, during append, during scoring and during finalization preserve exact call counts/history/pending status. No future validation token enters an earlier update and no new call or scientific update occurs after a global refusal. |
| `GenericReader.fit`, `PrescribedReader.fit`, `DiagnosticSession.retain` | Source table exports, validation/copy work, prescribed Phi and result materialization can precede reservations. | Reserve the full copied T/Phi, source hash/codec workspace and partial-fit/result owner before fit or provenance materialization. Same-T identity is retained. | ORDER0/1/2, selected and every deletion fit the identical permitted table; unavailable fits remain nonnumeric, with allocated prefixes and cause retained. |
| `replay_acquisition` and its `capture()` | Deep-copied source/final state, physical histories, trace records, two boundary T/Phi copies and later exported copies coexist. | Reserve replay learner and each disjoint copy separately before capture/append/export; all share the global accounting ledger. Do not count only the larger of two meter peaks. | Every RESET/update/credit-finalization boundary and snapshot hash matches the actual source. Interrupted internal043 cursors remain explicitly unsupported unless an exact new checkpoint schema is implemented and tested. |
| `trial_decomposition`, `choose_goal`, `partition_controls` | Up to150 decomposition readers remain live until session close, in addition to controls and replay. Predictions, exact-score intermediates, logs and unavailable-result dictionaries need workspace/terminal owners. | Reserve each actual reader and its lifetime plus bounded forecast/score/result workspace. Charge rational operands from their input widths, including reduction and equality comparison. Pending/unavailable output consumes prepaid finalization capacity. | All scored boundaries, zero gated-credit count effects, numeric prefixes, unavailable forecasts, four deletions and exact `raw=count+partition` identities are preserved. No diagnostic result returns to acquisition. |
| `serialize_retained`, compact diagnostic views, progress and row/report output | Payload exports, uncompressed bytes, compressed bytes, base64 bytes and text, hashes, readable summaries and outer JSON overlap. Several existing reservations follow allocation. | Cover both durable evidence and temporary serialization products before creation. Use a proved bound for any retained codec/compression workspace, or a preallocated bounded streaming transport with identical decoded evidence. Keep returned archive owners live. | Byte-identical recovered source payload, complete numeric rows and fixed-name transport hashes; size overflow preserves a bounded explicit incomplete record. Decoded/source length alone does not certify compression workspace. |
| Later042 evaluator, aggregation and verifier | The046 chooser uses fabricated histories and does not execute the physical target evaluator or final scientific tables. | Add a source-separated evaluator adapter with prepaid11-call decision evidence, frozen reader state, opaque history/goal interface, outcome recording, exact aggregation and final serialization. No target learner is needed for adapter conformance. | Finite fabricated interfaces check all11 call positions, unavailable choice behavior, no fitting/assimilation, per-reader counts and unchanged frozen state. Verifier fixtures check missing-outcome intervals, paired availability and042 continuation predicates. |
| Meter rejection, release and reporting paths | Refusal lists and exception-result objects can grow after exhaustion; ordinary `release` can itself refuse. | Prepay bounded emergency/footer/cleanup capacity. Latch the first terminal scientific work/memory refusal. After it permit only specified paid evidence commits and idempotent cleanup; neither replenish allowance nor perform cheaper scientific work. Meter metadata has its own finite apparatus bound. | Boundary fault injection yields one authoritative terminal cause, nonnegative owner totals, no lost issued/returned event, no work refund, no new scientific execution, and a bounded parseable report. |

## 4. Concrete deliverables and validation order

1. **One machine-readable contract and source map.** Freeze the allowed schemas, per-field cardinality/string/integer bounds, sharing policy, primitive work rules, stage multiplicities and owner lifetimes. Every table row above must be marked covered or blocked with a specific source location. An unclassified owner or operation prevents completion.
2. **One new accounting kernel and guarded adapter revision.** Use a global ledger with explicit scientific acquisition, diagnostic/evaluation and bounded apparatus subaccounts. Each simultaneously stored copy is counted once per actual independent copy, with no inferred sharing discount. Keep guarded operations and terminal commits source-separated from the target world implementation.
3. **One finite static projector with a simple reference.** A slow literal structural recurrence computes schema bytes, maximum overlap and work bounds from declared metadata. A faster algebraic form must match it on finite boundary cases and dominate every corresponding object/category in all preserved046 decoded rows. Archive traversal is a read-only projection audit, not a learner/profile rerun. Validate integer-width recurrences and terminal partial multiplicities explicitly. A maximum observed row alone is not this reference.
4. **One frozen conformance matrix.** Use the existing small043–046 semantic cases plus the table's finite new allocation/refusal seams. Compare sufficient-budget semantic outputs to the frozen reference. Inject refusal at every newly introduced guard class and the specified physical-call boundaries. Preserve the first failure and correction history. Tests must establish the contract, not merely assert constants copied from the implementation.
5. **One admission dossier or a located stop.** Publish actual correction identities, operation/source coverage, validated projection, separate budget derivations, physical feasibility scope, verifier, immutable preregistration and relevant programme fields. If any required envelope, operation class, pending behavior or projection remains unproved, label the implementation unadmitted and stop this correction route under the programme's revision policy. Do not create another speculative diagnostic/profile sequence to avoid that result.

The static schema/reference audit and archive projection can be completed without target or learner execution. They can establish conservative logical bounds only after the actual corrected schemas and guard rules exist. This047 assessment has not implemented those rules or run those checks and therefore supplies **no validated numerical total envelope**.

A material change in execution introduced by the correction must satisfy042's existing physical-validation rule; it cannot inherit046 timing as if newly measured. Resolve that precise dependency in the fixed correction validation plan before any run. Do not automatically repeat the48-row046 campaign, pick a favorable row after observing results, or launch an arbitrary replacement profile. Previously measured046 physical costs remain evidence for their exact frozen source and declared fabricated workloads.

Existing retained acquisition outputs cannot be retroactively re-metered into a valid **measured** scientific budget merely by scanning their final records. Any offline projection must first validate the changed ruler, intermediate multiplicities and complete operation trace/bounds; any changed physical path requires its applicable validation. A final-state scan is neither a measurement of omitted operations nor proof that the old run would have respected the new pre-expansion guards. `configs/P3_ACCOUNTING_COMPLETION_REQUIREMENTS_047.json` is the machine-readable pending checklist for this contract; it is not an implemented verifier or a new scientific threshold.

## 5. Fixed completion and remaining admission gates

Accounting correction is complete only when **every source-coverage row is guarded before expansion; every simultaneously retained object class, workspace and terminal output has a validated finite owner bound; work/bit prices cover actual required scientific operations; sufficient-budget semantics match the frozen reference; all required refusal cases preserve exact pending evidence; and the complete-work projector has passed its reference/archived-graph checks**. A successful physical run does not replace any of these conditions.

After that,042 still requires exact prospective common acquisition allowances and separate common diagnostic/readout allowances from eligible complete-work evidence/projection, its factor-two reserve, and the full grid plus verification planning projection with25% audit/rerun reserve. Preserve the world panel, B_env285, controls, outcome/availability rules and interpretation thresholds. Record which measured repetitions and projected cases set each maximum. A numerical allowance permits explicit resource abstention; it is not a guarantee that every target run completes.

Complete the preregistration, programme record and machine-checkable verifier, then obtain the **restricted independent review under a real separately metered allowance** already required by042. Historical native usage is unchanged by this assessment. A PI-authored specification, shared-workspace audit, profile completion or accepted accounting test is not an independent GO. Target treatment remains unadmitted until the applicable gates pass.

This is a bounded acquisition/short-history calibration branch. Neither an accounting correction nor a favorable future calibration ranks N1–N3 as a field-defining advance in recombination or retention. No additional paper is required to settle these local implementation dependencies.

## Inspected sources

The preceding analysis reads `docs/P3_COMPLETE_WORK_PROFILE_SCOPE_046.md`, `docs/P3_RESEARCH_PRIORITIES_045.md`, `docs/P3_REFERENCE_ACCOUNTING_043.md`, `docs/P3_REFERENCE_ACCOUNTING_DRAFT_043.md`, `docs/P3_COMPARISON_DESIGN_042.md`, and the frozen `scripts/accounting043.py`, `reference_n1_043.py`, `reference_n3_043.py`, `diagnostics045.py`, and `fullwork046.py` at the commit stated above. The draft043 binary-codec/arena alternatives were inspected as historical proposals, not adopted implementation. The separate047 return assessment supplies receipt/file verification and actual046 physical measurements.
