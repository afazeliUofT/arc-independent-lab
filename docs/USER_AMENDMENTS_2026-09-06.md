# Accepted user amendments — 2026-09-06

Authority: Ali's conversation reply to the consolidated intake, ending “Begin Phase 1.” This is a faithful decision record, not an invented approval or an independent verdict. The lab did not exist when these answers were supplied, so the pre-lab conversation answer channel applies.

- Hosted working copy authorized at `/workspace/scratch/d2a3782c2235/ARC_Independent_Lab/`. Laptop execution belongs under `~/ARC_Independent_Lab/`. GitHub bridges them. Containment and separation from the other programme remain mandatory.
- Scratch is ephemeral. Ledger, state files and every deliverable must reach GitHub. Natural checkpoint pushes are acceptable in Phases 1 and 2. Solve durable experiment-artifact transfer before Phase 3.
- Exact repository: https://github.com/afazeliUofT/arc-independent-lab . The user made it public and empty and authorized the GitHub connection. Ali executes the commits and pushes prepared by the agent. Never put credentials in messages or files.
- No hard end date. Scientific reviews at the end of Phase 1 and Phase 3. No additional spending, API charges, credits or rented compute.
- User-reported plan/model: OpenAI ChatGPT Ultra, GPT-6 Astra Ultra, shared with light personal use. Numeric allowance unknown. Establish authoritative observability at Gate 0; if unavailable, label the limitation and pause unattended model use. Do not substitute elapsed time or token counts for a usage percentage.
- Laptop full use authorized, including overnight. Reserve roughly four cores when the other programme is running. Its stated restart is Friday 2026-09-11 at 17:00 local; the user is in Toronto. Treat the schedule as a resource constraint only, never a reason to inspect that programme. Hibernate occurs after approximately 12.9 hours on AC; any machine-wide change needs a specific proposal and human decision.
- HPC available and encouraged. Allocation limits unknown; assume default-tier until measured using read-only commands run by Ali.
- Collaboration is asynchronous, with no guaranteed turnaround and longer gaps overnight/weekends in Toronto. Batch paper requests and submission bundles.
- All seven intake defaults accepted. Scientific framing claims are uncited user hypotheses to investigate with active suspicion, including human/animal ease and claims about scaling. Narrowing or refuting them is a result.
- Unattended continuation, subscription metering and reviewer isolation remain unverified until measured. Same-tools/same-filesystem sub-agents are not independent reviewers.
- Phase 1's small failure reproduction may run in the hosted sandbox. Preserve code and trace in GitHub; do not put the laptop or cluster in this critical path unnecessarily.

For all later human blocks, retain the governing file protocol: the human appends a section beginning `## ANSWER` to `state/ESCALATION.md`; the agent archives that answered file and records the decision. Do not silently substitute chat approval once the lab exists.
