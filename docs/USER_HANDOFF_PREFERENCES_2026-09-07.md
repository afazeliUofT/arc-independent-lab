## Handoff folder and repeat-run preference — 2026-09-07

User requests Bash/WSL commands opening the folder to copy downloaded files and the exact folder containing the report. Include both in future local handoffs. User reports repeating whole blocks after push problems; separate publication retries from measurements and make finite observation steps return an already completed, verified report without rerunning where practical. This does not authorize deleting prior runs or infer how many executions occurred.
