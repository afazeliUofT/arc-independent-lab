# Daily digest — 2026-09-08

Phase 3 remains in progress. The accepted diagnosis, generated candidates and PI novelty audit are unchanged; the separate reviewer has not issued a verdict and no final mechanism is selected.

The human sign-in receipt is accepted against the approved wrapper and scope. Acceptance: `evidence/GATE0_HUMAN_SIGNIN_015_RECEIPT_REVIEW.json`, SHA-256 `c66502fc2a3d940627ea7574a4bde1eaa89abef010bf5a00aff121f457280cfe`. One finite post-login metadata operation is now prepared. It checks a dummy-file boundary before permitting the pinned native client to read/update its existing credential file. Parent Python does not read or copy credentials.

Exact source showed a risk in combining live networking with read-only credentials: a token refresh can complete before persistence fails. The minimum supported save uses an existing-file writable bind, leaving the directory read-only. The original sign-in approval did not cover a subsequent native process's credential writes, so the concrete extension is recorded in `state/ESCALATION.md`. This is a real permission boundary, not another broad machine inventory.

The hosted namespace test failed before child startup because a required proc setting is unavailable. The failure is preserved. Targeted filtering, protocol, refusal and orchestration tests pass; evidence is `artifacts/GATE0_POSTLOGIN_ENGINEERING/20260908_001/MANIFEST.json`, SHA-256 `63435cd1fa5239bfc9d3fa45f8af57c7d8aae87d99455fef871449c49beb9e1c`. Actual native metadata and the final scientific reviewer boundary have not been tested. The same WSL invocation performs its prerequisite, so it needs no separate report round trip.

No model was invoked by the prepared operation or tests, no spending was added, and no allowance percentage was inferred. Authoritative hosted-model usage remains unavailable; unattended use remains paused. Engineering has consumed attention without adding scientific evidence. The next scientific task is still independent review of the frozen audit, followed by either experimental design for a survivor or a new acquisition-focused ideation pass.

Next human action: publish checkpoint016, record the exact narrow scope answer, run the combined check and provide only its report. The handoff includes folder-opening commands and keeps publication retries separate from measurements.
