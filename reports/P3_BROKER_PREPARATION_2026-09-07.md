# Reviewer preparation and the next scientific decision

2026-09-07. The Phase 3 novelty audit remains PI analysis awaiting separate enforced review. No candidate received a formal verdict, no new mechanism was invented or tested, and no new empirical claim about general AI competence was established here. The original objective and scientific branches remain those in `reports/P3_OBJECTIVE_AND_NEXT_DECISION_2026-09-07.md`; the approved diagnosis/ideas and both frozen review inventories remain unchanged.

## What is now implemented

The evidence broker supplies manifested text, manifested page images, hashes computed from actual complete bytes, the sole unchanged historical arithmetic auditor, and an exclusive structured verdict channel. The verdict channel validates the two review-manifest pins and separate C1–C4, joint and observer dispositions; every substantive verdict requires the actual fixed audit receipt. It does not choose a verdict or grade the reasoning. A dependency verdict may state that execution was unavailable. No real verdict was issued in development.

The protocol adapter binds calls to an externally supplied thread/turn, rejects replay and all other server request families, applies finite message/work limits, and emits the exact embedded client response shapes. Dynamic tools are additive; this adapter does not intercept native client tools automatically. The actual surrounding process still has to enforce all retained capabilities.

The unified private packet preserves originals, source hashes, complete extracted text and the already rendered scanned-method pages. Some original PDF pages are only accessible through text derivatives plus the original file hash; arbitrary visual page rendering is not yet a broker operation. A reviewer must report an additional visual dependency when it matters. Copyrighted material is not included in this public checkpoint.

The final broker/protocol validation covers 54 test cases across the recorded suites and an actual four-operation integration against the 148-file packet. The unchanged historical auditor checked 6,912 transitions on its original inventory; later metadata remained separately checked. Evidence for these counts and actual outputs: `artifacts/P3_REVIEW_BROKER_VALIDATION/20260907_003_aad8feb3/REPORT.json`, SHA-256 `f694367f848a1d58adcf0347152ed9f58e78d8a5f9123e9de990d26a15d68a45`. A later narrow addition includes the delivery-directory configuration origin in the launcher inventory; final launcher checks and exact source hashes are `artifacts/GATE0_CLIENT_PREFLIGHT_HARNESS/20260907_001/REPORT.json`, SHA-256 `99ca15258e3fec7ccedfba254409358ae3a79f314bf61a3c815a3020aebf7a53`. Earlier passing outputs were reused only for unchanged sources, and the failed isolated-import attempt was preserved.

## Errors and corrections

An initial source inference said app-server startup required a host-write exception for its installation identifier. Further source inspection found a supported private backing-file mount retaining the same identifier, preserving the host original and actual policy/auth sources. That necessity claim is explicitly withdrawn in the startup design. No exception was requested and no identity was regenerated.

The first mount-planner test failed under isolated Python because its test import assumed the normal script search path. The test now imports the exact sibling by file location; the production launcher already used that mechanism. A later supposedly fresh validation directory contained a pre-existing report and logs; exclusive creation stopped rather than overwriting them. The cause of that provenance discrepancy is unestablished. Those bytes are preserved, and final validation ran successfully in a fresh UUID-suffixed directory. See `evidence/P3_BROKER_VALIDATION_PROVENANCE_OBSERVATION.json`; the final validation cited above is the operative record.

Source-path guesses returned 404s before exact release-tree discovery; one upstream LICENSE was a symlink, so its dereferenced contents did not match that symlink's Git blob. The regular license target was then captured and verified. These are retrieval/engineering corrections, not scientific evidence or failures of the installed client. A proposed optional JSON Schema dependency was unavailable; no dependency was installed. The emitted scalar control paths/types/enums are checked against the pinned schema with the tested narrow stdlib validator.

## What remains

The human now runs the single bounded, network-disabled startup observation in `docs/GATE0_CLIENT_PREFLIGHT_RUN.md`. Existing authorization suffices. A passing response-only observation still leaves effective managed policy, all native/broker capabilities, fresh context and an existing-subscription reviewer model to establish before science. Authoritative allowance remains unknown and unattended model use is disabled. Shared-workspace engineering consultants are not independent reviewers.

Once a real reviewer is available, the task is to interrogate the reductions and the incomplete joint specification, including their strongest objections and limits. If those criticisms survive, the rational next research direction is the still-open acquisition of useful task descriptions/relations, with the proper phase transition recorded. This is a conditional PI preference, not a verdict or a silently changed research objective. No present justification exists for a GPU treatment grid, and cluster allocation/performance measurements remain deferred until a concrete experimental design needs them.
