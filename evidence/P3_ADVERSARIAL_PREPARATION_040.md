# Adversarial check before the corrected focused review

Date: 2026-09-12. This is PI engineering and scientific-scope preparation under C5. It does not replace an independent verdict.

## Strongest argument: the producer and consumer still disagree

The039 canary validated only a one-tool subset; separate unit checks falsely encouraged confidence in the full scientific route. The actual return shows that the full list failed before thread creation. This repeats the earlier030/032 lesson that fixtures built around one side of an interface can miss the production mismatch.

Required test: construct the exact current broker's full dynamic specs and the actual profile through their production functions; pass them through the actual admission helper before any native process. Reproduce the original refusal with the unchanged022 helper, demonstrate narrow040 acceptance, and reject unknown tools, the obsolete observer, duplicate tools, altered nested schemas, altered descriptions and arbitrary subsets. Also establish that the new session differs only in its intended admission import and emitted session kind. Results and exact source/test hashes are recorded in `evidence/P3_ADMISSION_REPAIR_VALIDATION_040.json` before release. The preflight is an execution prerequisite, not merely a preparation test.

## Second argument: a guarded stop could be mistaken for a scientific answer

In039 the stage-start flag was true but no scientific thread or turn began. A status summary that treated this as reviewed evidence would manufacture a disposition for R1-R7. The original return verifier re-executes the frozen collector and distinguishes stage entry, thread creation, turn submission, source delivery and verdict output. None of the scientific propositions changes because a canary passed. Future040 admission still requires a real scientific verdict, valid per-stage boundary receipts and exact packet measurements.

## Third argument: continuation could erase consumption or weaken isolation

The failed039 native starts count even though the scientific model was never called. A new version must bind those exact original receipts, preserve prior counters and forbid reuse of the old attempt. A tool-name repair must not become an open-ended allowlist expansion.040 binds only the existing exact broker contracts and preserves model, profile, permissions, source packet and return restrictions. Exclusive attempt reservations and publication-only retries remain mandatory. Token usage and charge remain unknown where the original receipts did not retain them.

## Scientific adversaries remain unchanged

The review must still distinguish finite fresh credit from complete learner failure, conditional count-fitting examples from reachable whole-loop histories, local greedy selection identities from complete learner equivalence, and endpoint distinguishability from knowledge useful for a future decision. Readiness engineering supplies no evidence of efficacy, novelty or broad applicability. The packet and brief remain frozen so the technical correction cannot tune scientific premises to obtain a preferred verdict.
