# Checkpoint046 pre-execution plan

Recorded 2026-09-13 before any new046 learner execution. The exact returned045 report at commit72f0792d242bfe19e59a0ed765d09748879926d6 passes the frozen045 validator:23tests/all13families, identical structured diagnostic results, one invocation and no target or model execution. Full source/receipt verification is in P3_CHECKPOINT_045_RETURN_VERIFICATION_046.json.

The single advancing046 step is a consolidated outcome-blind resource-profiling implementation and its finite fabricated coverage specification. It must include all four acquisition arms using the full W2/S3/K4/L5/V2/q4 configuration, chronology replay, prescribed readers, every selected-predicate deletion, all scored-trial P00/P10/P11 diagnostics and20 fabricated five-action decisions per required readout. Small/typical/largest acquisition sizes, three repetitions, and separate geometry/arithmetic/retained-state stress cases are frozen before full profiling. No042 target world, evaluator panel or target score may be imported into a worker. Frozen035,041/042 scientific design,043–045 implementation and reports remain unchanged.

A static source/coverage review must explicitly distinguish complete required-path execution from proof of maximal geometry, comprehensive instruction accounting and exact Python allocation. The current043 Meter and045 adapter limitations cannot be erased by a successful profile. B_comp/B_mem remain unset until eligible coverage has been justified under the prospective042 rule; no target admission or independent scientific verdict is part of046.

Hosted validation may execute the focused046 conformance suite and one bounded small integration of each materially distinct new path under a300second outer cap, one CPU and2GiB per-process address space. This is not a full resource measurement. Any failure and correction is retained. Before each actual validation, freeze its source identities and finite fixture/configuration. Before publication, derive the user profile's own finite operational allowance from source-level work projection and the available043–045 measurements; do not blindly reuse the old300second cap or knowingly request incomplete coverage. Preserve per-case reservations, incremental partial measurements, source/host identities, output bytes, wall/process/descendant CPU and peak memory. Repeating the WSL command reuses the consumed release reservation and retries publication without remeasurement.

The complete ZIP contains all required public sources and the preserved private packet; only approved report/receipt paths enter Git. No native reviewer/client, model API, GPU or HPC job is started by this development step. Native cumulative usage remains18starts/14sentturns. Hosted PI consultants are not independent reviewers; their backend usage/charge is unknown. Full-comparison laptop/Alliance routing remains open until the returned evidence supports a choice. No further paper is needed for this implementation/resource dependency.
