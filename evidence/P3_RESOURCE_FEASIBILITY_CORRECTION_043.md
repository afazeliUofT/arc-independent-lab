# Precision correction to the043 resource derivation

2026-09-12, before any full component profile. The original resource analysis is preserved at SHA-256 `562303c980e48f0bc2dd82568207488f2c066fce26cdf944a20deae3738f60b1`, including its pre-execution plan reference.

In section3, replace the equality `n = c-(5+r)` with the upper bound `n <= c-(5+r)`. Equality holds when every issued primitive call returns a completed response and enters the counted trace. An issued call may instead be interrupted without supplying a transition. The derived upper bound245 is unchanged.

The sharp bounds n245, F55, M45 and at most39 proposal assemblies apply to a fresh single-block042 acquisition with one-shot execution, completed-word exclusion, positive fresh-trial macro credit and termination after interrupted validation. They are cardinality/event bounds, not numerical work or memory ceilings. Repeated truncated validation followed by continued acquisition would be a different policy and cannot inherit them.

The actual development code uses named aggregate work charges and canonical JSON owner reservations. Its codec can construct temporary normalized objects before owner reservation; meter bookkeeping is outside scientific retained owners. Report those values as owner/reservation measurements alongside physical RSS/address space, not as a proof bounding every live allocation. This precision correction follows code inspection, not candidate or profile outcomes. No target result was generated.
