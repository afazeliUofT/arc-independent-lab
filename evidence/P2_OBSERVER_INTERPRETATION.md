# PI interpretation of the required observer follow-up

2026-09-06. The human approval requested recovery limited to what the learner could compute. Full prospective protocol, frozen inputs, source, raw trace and outcome: `reports/P2_LEARNER_OBSERVER_001.md`, SHA-256 `5503b185549fcce4daa841a9f4445f53d4f94dc3a43a73440bf877b2d38d1f11`. This note supplements the approved diagnosis without altering it.

**The main answer is positive.** A generic projection computed through the learner's own callable feature map recovers its old boundary estimate from its current weights. It is not given an old label, a saved boundary estimate, an arm identifier or the original coordinate-difference formula. The experience-dependent persistent state remains the original pair of weights. The supplied model, fixed features, zero initialization and known sequential update contract remain part of the prior structure. Outcome source: `artifacts/P2_LEARNER_OBSERVER/20260906_001/results.json`, SHA-256 `9c6c95c115e9d350e26c43707092c65208020eac9b02b91d389d335fa3febd08`, local access 2026-09-06.

This corrects an ambiguity in the earlier caution. Supplying the model's geometry to an analyst is not the only route to that geometry: the original learner already uses an executable map for prediction. The follow-up derives the required relation through that map rather than giving the reader the analyst's answer. The newly written reader remains a researcher-designed computation. Computational accessibility and the learner's autonomous discovery of a suitable computation are different claims; only the former is strengthened here.

The additional learned-span reader uses explicitly retained geometric memory. Its success is therefore not smuggled into the original-state result. Conversely, the ambiguous-history construction removes access to the other context's map as well as geometric memory. That smaller information view cannot identify both histories over the declared broader map class, but it is not the original model interface and does not refute the positive main result. The protocol and report state those changes before interpreting the comparison.

**Consequence for the ranking.** Rank 3 remains a well-established local cause with a now stronger access result: an available computation is missing from the ordinary response path. Its position below the two acquisition priorities concerned the funded conjunction and modern-agent relevance, neither of which this small experiment measures. I therefore retain the approved order. The funder's proposed success/failure split is decisive about a specified access condition, not automatically about general relevance. A negative outcome from one restricted reader would not have disproved all surviving information either.

The full raw idea population was generated without using this result to reject or promote entries. The diagnostic design was known during generation and is explicitly acknowledged in the B3 batch; it is not an independent source of mechanistic novelty. Candidate C3 addresses representation-to-reader translation, but its retained activation anchors are a different state allowance and cannot inherit this experiment's success. No candidate efficacy trial or independent scientific verdict occurred.
