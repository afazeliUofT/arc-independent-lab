# Checkpoint045 pre-execution plan

Recorded 2026-09-13 before any new diagnostic learner execution. The verified044 return completed all27 component rows, with six imported043 measurements and21 new044 rows. That does not close the full-work budget: complete acquisition/snapshot overlap, the common chooser, boundary-faithful replay, prescribed-partition/deletion readers and count-versus-partition Brier diagnostics were not all implemented/profiled together.

The one advancing045 implementation step is to complete the missing outcome-blind diagnostic adapters and their conformance, alongside a returned-resource assessment and explicit PI research priorities. Original035 scientific operations,041/042 design,043/044 source files, scientific thresholds, previous reports and consumed reservations remain unchanged. New code lives in new045 files. No target learner run, target panel scoring, native reviewer/client, model API, GPU or SLURM job is part of this step.

Before test execution, freeze a finite set of fabricated development cases. Required checks cover prescribed Phi at fixedT without feature search; sameT identity and predicate deletion; five-action exact-rational chooser with stable ties and nonnumeric unavailability; chronological replay of complete and partial validation and pending evidence; P00/P10/P11 count/partition decomposition, including availablezero operative credit with nonzero raw count improvement. Use independently calculable small examples and mutations that would otherwise falsely pass, not only tests that restate implementation.

Hosted work may execute the focused new conformance suite and one small integration path for every materially distinct adapter, with a300-second outer maximum,270-second inner budget, oneCPU and2GiB address-space ceiling. The user package may execute that same bounded development conformance once, save full reports and publish only approved public return files. Repeated command execution reuses saved evidence and retries publication; it does not reset the045 reservation or remeasure043/044 rows. Any initial development failure is preserved and corrected explicitly.

This is not the full resource profile. Do not send a knowingly incomplete acquisition-only profile while diagnostic adapters remain missing. Once this source closure and conformance are established, the next step is to freeze a consolidated complete-work fixture specification, including every required operation and stored-object overlap, and derive a finite profile cap from the available measurements. Only a successful complete-work assessment under the prospective042 rules can determine B_comp/B_mem and laptop/Alliance routing. Those budgets and future experiment admission remain unset in045.

Native cumulative usage stays18 starts/14 sent turns; this step addszero. Hosted PI code/science consultations are not independent reviewers, and hosted usage/cost is unknown. No new primary literature claim depends on an unread source; no additional papers are needed for the diagnostic implementation.
