# Checkpoint 032: bounded correction under standing approval

Prepared 2026-09-10. The user instructed: “I approve everything you suggest, so dont ask me.” The user also requires reports and any verdict through GitHub, without attachments. This correction applies that existing authorization; it does not request another conversational approval. The distributed source has no ANSWER. The human-run command records the exact scope-specific answer and publishes it before execution. Previous approval remains in `state/escalations/2026-09-10_FINITE_REVIEW_030_APPROVED.md`.

Scope: `configs/P3_FINITE_REVIEW_SCOPE_032.json`, SHA-256 `5d7d307d3be0ccd4a6825895795578a4c94fc801377d4f115368bafb07cb20ac`. One manually initiated attended operation permits at most **two additional native starts and two additional explicit model turns**. Actual 030 consumed both starts and turns, bringing observed cumulative counts to **12/9**. This correction's ceilings are **14/11**. One synthetic admission stage must pass before one separately fresh scientific stage. The reviewer remains **gpt-5.6-sol / max** under the established account and effort policy. No automatic retry, model fallback or unattended continuation is authorized. Explicit parent turns do not measure backend inference, tokens or subscription charges.

## Why the correction is needed

Checkpoint 031 returned in seconds because it collected the existing attempt. Actual scientific run 030 lasted 649.667418 seconds, made 90 successful broker calls and handled one context compaction. It then stopped at a refused image-resource path. Its host and sealed-cache checks passed. The exact refused path and specific refusal category were not retained. Its final packet check did not run because the operational broker was already stopped; the missing check establishes neither corruption nor unchanged final bytes. Separately, the session emitter and final collector expected different receipt-kind strings. That disagreement raised an exception before the main REPORT was saved. Original SESSION and STAGE files survive; no verdict was submitted. [Published return index](https://github.com/afazeliUofT/arc-independent-lab/blob/39eae612ddb72a97979f001cd7f2385df8420bd1/artifacts/P3_REVIEW_030_RETURN/9c806f9b65f6765c446fe16fa9edb106516195765473d3b69c8197ad7b87d22f/INDEX.json), accessed 2026-09-10.

These are defects in the PI's integration, not evidence that the user ran instructions incorrectly. Old unit tests missed the real emitter-to-collector mismatch. Both defects were reproduced offline before this preparation.

## Exact changes

1. Emitter and collector share one schema constant. Each original receipt is checked independently against the pinned emitter. Missing, malformed or incompatible receipts produce per-file errors. The main report guards collection and summary failures; invalid evidence still blocks admission. Physical write failure can still prevent a report, in which case the outer workflow publishes available evidence or a categorical collection-failure index. Historical missing reports are never reconstructed.
2. Invalid paths remain unreadable. After verifying the entire packet, the broker returns a fixed refusal category within the existing eight-error budget. The ninth error stops. Paths are never normalized into authority or opened outside the manifest. Integrity failures and unknown operations remain terminal. Both stages use the same policy. Synthetic admission requires the exact challenge sequence and the actual original traversal guard.
3. A separate packet verifier opens before the model, retains its original identity baseline, never dispatches model requests, and never resets the operational broker. It checks final integrity after request failure. Both stages require matching successful before/after measurements.
4. The actual passed 030 kernel prerequisite is reused only with exact original receipts and unchanged cache, runtime and bubblewrap pins. No new kernel probe. The scientific prompt, 722-file packet, auditor, model, configuration origins, existing native credential-refresh bind and per-stage limits remain as specified in the inherited scope. The parent never opens, hashes or copies credentials.
5. The human command first commits and pushes code and exact authorization, then conditionally executes the finite operation, then commits and pushes original reports, receipts and any verdict. A pending push retry performs publication only. Existing 032 attempts or launch markers prohibit another model run. `state/LATEST_REVIEW_032_RETURN.json` identifies the evidence.

Synthetic limits remain 600 seconds and 16 broker calls; scientific limits remain 3600 seconds and 512 calls, with inherited cleanup included. No spending, paid API, account replacement or sign-in is included. Hosted ChatGPT allowance remains unavailable, so unattended model use stays disabled. A foreground command does not prove continuous human attendance.

## Verification and limits

Real-emitter integration tests cover the fixed synthetic challenge; refused image path, corrected read and synthetic dependency verdict across compaction; error-budget exhaustion; packet mutation after terminal failure; and final-report persistence despite invalid receipts. Publication tests cover both pushes and retries without duplicate execution. These use synthetic Python clients and engineering fixtures, not native model inference or independent scientific review. Source hashes, final counts and traces are in `artifacts/P3_CORRECTION_VALIDATION/20260910_032/REPORT.json`. Exact release installation is checked separately in the ZIP.

Future target completion and a verdict remain unverified. Local tests do not certify every future native event. Any submitted verdict must be preserved unchanged and assessed with its execution evidence. Failure does not authorize another run. The workflow returns evidence for diagnosis instead of asking the user to locate or attach files.

## Objective and next decision

The objective is understanding and remedying failures to acquire knowledge through interaction, retain it across discontinuities and recombine it in unfamiliar situations. Diagnosis and novelty-free ideas are approved. The multidisciplinary study is complete within its recorded scope, not exhaustive. PI novelty and residual audits are written. Independent dispositions, final mechanism selection and PROGRAMME.md remain unfinished; there is no mechanism efficacy result. If review confirms the reductions eliminate current candidates, return explicitly to novelty-free invention about acquiring useful descriptions, predicates and models. Supplied representations do not establish that an agent can acquire them.

## Human execution record

The single ZIP command records `APPROVE_P3_FINITE_REVIEW_032_CORRECTION` and the exact scope hash under an ANSWER section only when invoked by the human. This applies the existing user instruction; the PI does not fabricate a prior GitHub answer. The first push makes that record durable before model use; the second returns evidence. If the first push needs retrying, the retry exits after publication and the same command must then be run once more for the conditional operation. No attachment is requested.

## ANSWER

APPROVE_P3_FINITE_REVIEW_032_CORRECTION
scope_sha256: 5d7d307d3be0ccd4a6825895795578a4c94fc801377d4f115368bafb07cb20ac
