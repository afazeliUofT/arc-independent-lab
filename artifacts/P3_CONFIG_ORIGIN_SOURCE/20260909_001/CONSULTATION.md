# Configuration-origin contract consultation

Engineering consultation, not an independent scientific review. Access date: 2026-09-09. Native source is pinned to Codex 0.151.0 commit `78c290807ce710180111df227df3b7a4fe845452`.

The submitted repair report establishes a successful `config/read` response followed by a local configuration-origin admission refusal. It establishes no thread, model turn, broker canary or scientific review. Its lack of `science_output` and a verdict is expected. The old controller did not preserve a safe per-control origin observation, so the exact first received failing path is not recoverable from this report alone.

Two defects are independently established by the exact source, and reproduced with a source-derived synthetic response in `OFFLINE_REPRODUCTION.json`. Both occur even when the requested restrictive controls are supplied by the enabled session layer. The old test fixture creates origins by calling the validator's own `_leaves`; this made it agree with the implementation's erroneous representation instead of the native contract.

| Controlled input | Old expected origin | Actual native origin |
|---|---|---|
| `features.multi_agent_v2=false` | `features.multi_agent_v2` | `features.multi_agent_v2.enabled` |
| `features.code_mode.direct_only_tool_namespaces=["functions"]` | `features.code_mode.direct_only_tool_namespaces` | `features.code_mode.direct_only_tool_namespaces.0` |

The native `record_origins` function descends dictionaries and arrays, records scalar leaves, and records no origin for an empty collection. Boolean structured features use an `enabled` leaf, with one compatibility exception: `network_proxy` records both the boolean path and its `enabled` path. `is_structured_feature_path` recognizes only `multi_agent_v2` and `network_proxy`, at the top-level features path or under a named profile. It does not transform boolean `code_mode_host`, nor general false features. The array behavior applies to every array, not just this one namespace list. Sources: [fingerprint.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/fingerprint.rs), [merge.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/merge.rs), accessed 2026-09-09.

The surrounding assumptions were checked rather than replaced wholesale:

- `ConfigReadResponse.layers` contains all non-packaged layers, including disabled layers, highest precedence first. `origins` traverses only enabled layers, low to high, and the API removes packaged-default origins and origins covered by exact requirements. A missing origin must still stop when a required session winner cannot be verified; it must not be silently attributed to that session. Requirements remain separately checked. Source: [config_manager_service.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/app-server/src/config_manager_service.rs), [state.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/state.rs), accessed 2026-09-09.
- The session-layer wire name is exactly `{ "type": "sessionFlags" }`. Origin metadata has `name` and `version`, and uses the same version as its source layer. The native version is `sha256:` plus the SHA-256 of canonical JSON for the raw TOML layer. Existing exact name/version correspondence checks are correct. Layer precedence is packaged -10, MDM 0, system 10, enterprise 15, user 20 or selected user profile 21, project 25, session 30, legacy managed file 40, legacy managed MDM 50. Higher enabled layer shadow checks remain necessary. Sources: [config_layer.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/app-server/src/config_layer.rs), [config_layer_source.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/config_layer_source.rs), [protocol configuration types](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/app-server-protocol/src/protocol/v2/config.rs), accessed 2026-09-09.
- Native key aliases normalize only `memories.no_memories_if_mcp_or_web_search` to `disable_on_external_context` and `agents.max_threads` to `max_concurrent_threads_per_session`. Neither is a requested reviewer control. No broad dotted-key normalization or aliases are needed for this profile. Source: [key_aliases.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/key_aliases.rs), accessed 2026-09-09.
- The projected API serializes `ConfigToml` before reading it into `ApiConfig`, whose flattened additional map preserves features and other unmodeled top-level settings. The already audited two nested tool controls are omitted by `ToolsV2` and still need their existing enabled-layer/origin verification. `FeatureToml` is untagged. Boolean `multi_agent_v2=false` remains boolean unless merging into an existing lower structured table, in which case native merge retains that table and changes `enabled` to false. This is a source-permitted representation, not evidence that the submitted run returned one. The accepted prior metadata report already records this machine's projected `multi_agent_v2` as equal to requested false. Sources: [feature types](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/features/src/lib.rs), [merge.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/merge.rs), accessed 2026-09-09.
- `CodeModeConfigToml` omits unset optional fields on serialization. Equality of the intended full `code_mode` table remains a valid deliberately strict requirement: unexpected retained settings must not disappear merely because `enabled` is false. `code_mode_host` must remain literal false because the host configuration's table can retain a distinct fallback setting. Sources: [feature_configs.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/features/src/feature_configs.rs), [merge.rs](https://github.com/openai/codex/blob/78c290807ce710180111df227df3b7a4fe845452/codex-rs/config/src/merge.rs), accessed 2026-09-09.

The smallest source-correct correction changes origin-path generation to match scalar/indexed native leaves and the narrowly enumerated structured boolean transformation, while retaining exact session-layer values, exact source name/version correspondence, no higher enabled shadow, projected equality, instruction checks and separate requirements admission. It does not alter the requested profile, native privileges, model choice or scientific input. The correction deliberately retains a dependency stop for a projected structured `multi_agent_v2`, with an explicit policy diagnostic; the previously observed machine returns the boolean form. Changing all tables to an `enabled`-only check would be incorrect.

Recommended regression cases are independent literal native-shaped origins; every indexed array item requiring its own origin; missing or wrong-version indexed origins; `multi_agent_v2.enabled` missing or attributed to a higher layer; same-value higher shadow; replacement ancestors; disabled higher layers; exact managed requirements removing an origin; null/malformed/unknown origin metadata; and unexpected effective Code Mode table members. Complete filtered per-control diagnostics should be retained before raising so another failure reports which fixed known control and which check failed without saving arbitrary configuration, paths, values, version strings or credentials.

This consultation does not establish that the corrected controller will pass actual native admission. That needs the next explicitly launched bounded run, under the existing review authorization. No native client, authentication operation or model call was performed here.

Implemented correction: `scripts/p3_reviewer_admission_021.py` leaves the pinned original unchanged. `observe_effective_config` returns a complete filtered observation for all controls before any control failure is raised; `validate_effective_config` retains its success contract and attaches the safe observation to its fixed local stop. The 21 dedicated offline tests passed; source-derived native admission remains unmeasured. Test fixtures use literal native path exceptions and do not invoke the validator to generate expected origins.
