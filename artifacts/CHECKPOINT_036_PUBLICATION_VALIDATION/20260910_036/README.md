This record validates the checkpoint036 publication helper, which installs and pushes declared documents only when the human runs it. The 22 offline tests use an in-memory Git fixture; no actual commit, push, reviewer, probe or model is executed. After the PI freezes the full payload, it must additionally be installed twice and exactly staged twice on a reconstructed fixture of the exact 1306-blob published baseline before delivery; that later readback is recorded inside the ZIP in RELEASE_VERIFICATION.json. This is operational validation, not an independent scientific review.
