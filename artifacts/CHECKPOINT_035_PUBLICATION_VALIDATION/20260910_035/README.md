This record validates the checkpoint035 publication helper, which installs and pushes declared documents only when the human runs it. The 22 offline tests use an in-memory Git fixture; no actual commit, push, reviewer, probe or model is executed. The full frozen payload is additionally installed twice and exactly staged twice on a reconstructed published-tree fixture before delivery; that later readback is recorded in the outer ZIP. This is operational validation, not an independent scientific review.
