# Derived verification of published run 032

The unchanged, source-pinned 032 controller accepts the exact copied original
REPORT, SESSION, STAGE and verdict bytes. Its original synthetic and science
admission predicates both pass. The exact preserved approval, all dependency
pins, receipt links, output digest, cumulative counts and reused actual030 kernel
receipt check pass. No native process, model turn, auditor or kernel probe ran in
this engineering check. Original archive files remain unchanged.

The scientific session ran for 1,045.474540 seconds, made 164 broker calls (161
successful and three bounded input refusals), completed one context compaction,
and submitted a verdict. The controller took 1,058.418730 seconds in total. The
observed cumulative count is 14 native starts and 11 sent turns.

The original reviewer requested and successfully completed the fixed observer
audit. The unchanged broker requires that success before accepting a substantive
verdict. Its receipt is reported as SHA-256
`81c4fa7c32b9a27a57dafb0e7675ef37024976e6e43668f4d48295f961a00d78`.
The fixed return allowlist omitted that auxiliary receipt, however, so this
verification does **not** claim its original bytes were retrieved or rehashed.
Capturing the existing receipt is a pending durability task; rerunning the audit
would create different evidence and is unnecessary.

This artifact records post-execution engineering consistency. It is neither an
additional independent scientific review nor external attestation of historical
execution. Private paper and page-image bytes are absent from the public return;
their historical verification is supported by broker/packet receipts. See the
explicit limitations and measurements in `VERIFICATION.json`.

The first disposable-mirror setup attempt failed because this rebuilt checkout
had no `delivery/` directory. Creating that owned directory resolved the setup
error; the full recorded verifier then passed without modifying original files.

Source: [published run return](https://github.com/afazeliUofT/arc-independent-lab/tree/main/artifacts/P3_REVIEW_032_RETURN/94ddb570582a75780f03e54df5e3228a58dc9de23397d63eac4976928b9a2d11),
accessed 2026-09-10. The content-addressed directory and per-file digests identify
the exact evidence; this document does not claim the branch remains immutable.
